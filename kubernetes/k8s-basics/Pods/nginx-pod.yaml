apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    # env:
    # - name: EXAMPLE_ENV_VAR
    #   value: env-var-value    
    ports:
    - containerPort: 80